import React from 'react';
// import { GlobalContext } from '../GlobalContext';

const ErrorMessage = () => {
  // const { setError, input } = React.useContext(GlobalContext);

  return <React.Fragment></React.Fragment>;
};

export default ErrorMessage;
